---
const { src, caption } = Astro.props;
---

standard html:
<figure>
  <img src={src} alt={caption}>
  <figcaption>{caption}</figcaption>
</figure>

astro syntax mixed into standard html:
<figure>
  ![{caption}]({src})
  <figcaption>{caption}</figcaption>
</figure>